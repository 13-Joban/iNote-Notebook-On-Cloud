import { createContext} from "react"
// we will make a context and we can access anything that is passed even if we have large number of components 

const Notescontext =  createContext();
export default Notescontext;