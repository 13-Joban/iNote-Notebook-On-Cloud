const express = require('express');

const router = express.Router();
// ROUTE- 1 -> Get all h
router.get('/', (req, res) => {
    //     let     obj ={
    //     name: 'joban notes',
    //     class: 'csb notes'
    // }
    
    // res.json(obj)
  })

module.exports = router